server:
  port: ${REST_SERVICE_PORT:8086}
spring:
  datasource:
    url: ${POSTGRES_URL:jdbc:postgresql://localhost:9100/fileSharingDb}
    username: ${POSTGRES_USERNAME:deekul}
    password: ${POSTGRES_PASSWORD:p@ssw0rd}
  jpa:
    generate-ddl: true

salt: ${SALT:test}